{
  "private": true,
  "scripts": {
    "update-protoplast": "cd ../.. && npm run build && cd examples/todo && npm install ../.."
  },
  "dependencies": {
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.0",
    "protoplast": "../.."
  },
  "devDependencies": {
    "d3": "^3.5.6"
  }
}
